<odoo>

  <!-- Acciones al abrir las vistas en los modelos 
       https://www.odoo.com/documentation/19.0/es/developer/reference/backend/actions.html
  -->
  <record model="ir.actions.act_window" id="action_lista_tareas">
    <!-- -->
    <field name="name">Listado de tareas pendientes</field>
    <!-- Nombre del modelo que mostraremos en la vista-->
    <field name="res_model">ej02.lista.tareas</field>
    <!-- indicamos que nuestro formulario tendrá vista list (para mostrar todos los datos
         y vista form (para crear o editar datos).
         La vista Form no la definimos, por lo cual tendremos la version automática.
         La vista list la definiremos mas abajo.
    -->
    <field name="view_mode">list,form</field>
  </record>

  <!-- Top menu item 
       En este ejemplo, todo el menu de "menuitem" esta puesto como ejemplo,
       no hace una función concreta
  -->
  <menuitem
      name="Listado de tareas"
      id="lista_tareas_menu_root"/>

  <!-- menu categories -->
  <menuitem
      name="Opciones Lista Tareas"
      id="lista_tareas_menu_1"
      parent="lista_tareas_menu_root"
      action="action_lista_tareas"/>

  <!-- actions -->
  <menuitem
      name="Mostrar lista"
      id="lista_tareas_menu_1_list"
      parent="lista_tareas_menu_1"
      action="action_lista_tareas"/>

  <!-- Odoo puede colocar vistas automaticas, aunque con un aspecto y funcionalidad mejorables.
       Nosotros, para afinarlo, explicaremos de manera explicita como es cada vista -->

  <!-- Definimos como es la vista explicita de la lista-->
  <record model="ir.ui.view" id="lista_tareas_view_list">
    <field name="name">Lista de tareas</field>
    <field name="model">ej02.lista.tareas</field>
    <field name="arch" type="xml">
      <list>
        <field name="tarea"/>
        <field name="prioridad"/>
        <field name="urgente"/>
        <field name="realizada"/>
      </list>
    </field>
  </record>

</odoo>
